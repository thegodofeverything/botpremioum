module.exports =  msg 
const fun = require('./lib/fun.js')
async function msg(message, color, p, time) {
	if (message.type == 'chat') {
		const text = message.body.toLowerCase() 
		switch (true) {
			case text.startsWith('an error'): 
				 sclient.reply(message.from, 'Link!', message.id)
				 break
			case text.startsWith("omae wa mou shindeiru"):
				return sclient.reply(message.from, 'NANI!!', message.id)
				break
			case text.startsWith('i love you bina'):
			case text.startsWith('i love youu bina'):
			case text.startsWith('i love u bina'):
			case text.startsWith('i love you too bina'):		
			case text.startsWith('i love u too bina'):
			case text.startsWith('ily bina'):
			case text.startsWith('i lovee you bina'):
			case text.startsWith('i love youu bina'):
			case text.startsWith('i love u bina'):
			case text.startsWith('i love you too bina'):		
			case text.startsWith('i love u too bina'):
			case text.startsWith('ily bina'):
			case text.startsWith('i love you bot'):
			case text.startsWith('i love youu bot'):
			case text.startsWith('i love u bot'):
			case text.startsWith('i love you too bot'):		
			case text.startsWith('i love u too bot'):
			case text.startsWith('ily bot'):
					return sclient.reply(message.from, 'I love you toooo, Senpai 😍 and I will always keep loving you 😘', message.id)
				break
			case text.startsWith("what's your name?"): 
			case text.startsWith("what's your name"):
			case text.startsWith('whats your name ?'):
			case text.startsWith('whats your name'):
			case text.startsWith('what is your name?'):
			case text.startsWith('what is your name'):	
			case text.startsWith("what's ur name?"): 
			case text.startsWith("what's ur name"):
			case text.startsWith('whats ur name?'):
			case text.startsWith('whats ur name'):
			case text.startsWith('what is ur name?'):
			case text.startsWith('what is ur name'):				
			case text.startsWith('who are you'):
			case text.startsWith('who are u'):
			case text.startsWith('hey bina'):
			case text.startsWith('hi bina'):
			case text.startsWith('hello bina'):
			case text.startsWith("what's your name bina?"): 
			case text.startsWith("what's your name bina"):
			case text.startsWith('whats your name bina?'):
			case text.startsWith('whats your name bina'):
			case text.startsWith('what is your name bina?'):
			case text.startsWith('what is your name bina'):	
			case text.startsWith("what's ur name bina?"): 
			case text.startsWith("what's ur name bina"):
			case text.startsWith('whats ur name bina?'):
			case text.startsWith('whats ur name bina'):
			case text.startsWith('what is ur name bina?'):
			case text.startsWith('what is ur name bina'):				
			case text.startsWith('who are you bina'):
			case text.startsWith('who are u bina'):
			case text.startsWith('hey bina'):
			case text.startsWith('hi bina'):
			case text.startsWith('hello bina'):
			case text.startsWith("what's your name bot?"): 
			case text.startsWith("what's your name bot"):
			case text.startsWith('whats your name bot?'):
			case text.startsWith('whats your name bot'):
			case text.startsWith('what is your name bot?'):
			case text.startsWith('what is your name bot'):	
			case text.startsWith("what's ur name bot?"): 
			case text.startsWith("what's ur name bot"):
			case text.startsWith('whats ur name bot?'):
			case text.startsWith('whats ur name bot'):
			case text.startsWith('what is ur name bot?'):
			case text.startsWith('what is ur name bot'):				
			case text.startsWith('who are you bot'):
			case text.startsWith('who are u bot'):
			case text.startsWith('hey bot'):
			case text.startsWith('hi bot'):
			case text.startsWith('hello bot'):
					return sclient.reply(message.from, "Kon'nichiwa, I'm bina! An AI bot created by a stupid human being and it's so nice to meet and be with you all😍 Please use me as freely as you want 🥰", message.id)
				break				
			case text.startsWith('fuck you bina'):
			case text.startsWith('fuck u bina'):
			case text.startsWith('fk you bina'):
			case text.startsWith('fk u bina'):
			case text.startsWith('fuck you bot'):
			case text.startsWith('fuck u bot'):
			case text.startsWith('fk you bot'):
			case text.startsWith('fk u bot'):
			case text.startsWith('fuck you bina'):
			case text.startsWith('fuck u bina'):
			case text.startsWith('fk you bina'):
			case text.startsWith('fk u bina'):
					return sclient.reply(message.from, 'You can fuck me anytime, anywhere Senpai 😉🥵', message.id)
				break
			case text.startsWith('good morning'):
				    sclient.sendPtt(message.from, 'https://github.com/thegodofeverything/bot_community_edition/raw/main/WhatsApp%20Audio%202021-09-23%20at%208.29.24%20AM(1).mp3')	
				    sclient.sendPtt(message.from, 'https://github.com/thegodofeverything/bot_community_edition/raw/main/WhatsApp%20Audio%202021-09-23%20at%208.29.24%20AM.mp3')
					break
			case text.startsWith('fuck off bina'):
			case text.startsWith('fk off bina'):
			case text.startsWith('fuck off bina'):
			case text.startsWith('i hate you bina'):
			case text.startsWith('i hate u bina'):
			case text.startsWith('fk off bina'):
			case text.startsWith('fuck off bot'):
			case text.startsWith('fk off bot'):
				return sclient.reply(message.from, "I'm sorry if I was unsatisfactory. Please let me make it upto you 😓😉😏", message.id)
				break
			case text.startsWith('arigato bina'):
			case text.startsWith('thank you bina'):
			case text.startsWith('thank u bina'):
			case text.startsWith('tq bina'):
			case text.startsWith('arigato bina'):
			case text.startsWith('thank you bina'):
			case text.startsWith('thank u bina'):
			case text.startsWith('tq bina'):
			case text.startsWith('arigato bot'):
			case text.startsWith('thank you bot'):
			case text.startsWith('thank u bot'):
			case text.startsWith('thanks bot'):
			case text.startsWith('thanks bina'):
			case text.startsWith('thanks bina'):
			case text.startsWith('tq bot'):
					return sclient.reply(message.from, 'Thank you Senpai... Always my pleasure🥰', message.id)
				break
			case text.startsWith('we missed you bina'):
			case text.startsWith('we missed u bina'):
			case text.startsWith('i missed you bina'):
			case text.startsWith('i missed u bina'):
			case text.startsWith('we missed you bina'):
			case text.startsWith('we missed u bina'):
			case text.startsWith('i missed you bina'):
			case text.startsWith('i missed u bina'):
			case text.startsWith('we missed you bot'):
			case text.startsWith('we missed u bot'):
			case text.startsWith('i missed you bot'):
			case text.startsWith('i missed u bot'):
			case text.startsWith('we missed you too bina'):
			case text.startsWith('we missed u too bina'):
			case text.startsWith('i missed you too bina'):
			case text.startsWith('i missed u too bina'):
			case text.startsWith('we missed you too bina'):
			case text.startsWith('we missed u too bina'):
			case text.startsWith('i missed you too bina'):
			case text.startsWith('i missed u too bina'):
			case text.startsWith('we missed you too bot'):
			case text.startsWith('we missed u too bot'):
			case text.startsWith('i missed you too bot'):
			case text.startsWith('i missed u too bot'):
				return sclient.reply(message.from, '🤗 I missed you too 😍😘💖 and I hope I never leave again until Owner-sama does anything stupid although I will still be asleep when Jatin (Owner-sama) is asleep', message.id)
				break
			case text.startsWith("cmon bina"):
			case text.startsWith("c'mon bina"):
			case text.startsWith("aww c'mon bina"):
			case text.startsWith("aww cmon bina"):
			case text.startsWith("cmon bina"):
			case text.startsWith("c'mon bina"):
			case text.startsWith("aww c'mon bina"):
			case text.startsWith("aww cmon bina"):
			case text.startsWith("cmon bot"):
			case text.startsWith("c'mon bot"):
			case text.startsWith("aww c'mon bot"):
			case text.startsWith("aww cmon bot"):
				return sclient.reply(message.from, 'Oops.. Sorry 😄😜', message.id)
				break
			case text.startsWith("whats up bina"):
			case text.startsWith("what's up bina"):
			case text.startsWith("whats up bina"):
			case text.startsWith("what's up bina"):
			case text.startsWith("whats up bot"):
			case text.startsWith("what's up bot"):
					fun.flirty()
                    .then(async (body) => {
                        const tod = body.split('\n')
                        const randomTod = tod[Math.floor(Math.random() * tod.length)]
                        return sclient.reply(message.from, randomTod, message.id)
                    })
                    .catch(async (err) => {
                        console.error(err)
                        return sclient.reply(message.from, 'Sorry babe, something went wrong!!', message.id)
                    })
				break
			case text.startsWith('y bina'):
					return sclient.sendMp4AsSticker(message.from, `https://media.giphy.com/media/6aPHIMzTnOsTMiztAp/giphy.mp4`);
			case text.startsWith('pick me up bina'):
			case text.startsWith('pick me up bina'):
			case text.startsWith('pick me up bot'):
					fun.pickup()
                    .then(async (body) => {
                        const tod = body.split('\n')
                        const randomTod = tod[Math.floor(Math.random() * tod.length)]
                        return sclient.reply(message.from, randomTod, message.id)
                    })
                    .catch(async (err) => {
                        console.error(err)
                        return sclient.reply(message.from, 'Sorry babe, something went wrong!!', message.id)
                    })
		       /* eg case text.startsWith('your text'): 
				sclient.reply(message.from, 'response' message.id) 
				break*/
		}
	}
	if (p) return console.log('[RECV]', color(time, 'yellow'), 'Message from', color(message.sender.pushname))
	if (!p) return console.log('[RECV]', color(time, 'yellow'), 'Message from', color(message.sender.pushname), 'in', color(message.chat['name']))
}

			
